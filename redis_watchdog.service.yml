services:
  logger.redis_watchdog:
    class: Drupal\redis_watchdog\RedisWatchdogLogger
    arguments: [@cache.backend.redis, @logger.log_message_parser']
    tags:
      - { name: logger}
      - { name: backend_overridable }
parameters: